version: '3'

tasks:
  build:
    desc: Build the mdthing binary
    cmds:
      - go build -o mdthing .

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -f mdthing

  install:
    desc: Install mdthing to GOPATH/bin
    cmds:
      - go install .
