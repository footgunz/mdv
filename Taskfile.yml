version: '3'

tasks:
  build:
    desc: Build the mdv binary
    cmds:
      - go build -o mdv ./cmd/mdv

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -f mdv

  install:
    desc: Install mdv to GOPATH/bin
    cmds:
      - go install ./cmd/mdv
